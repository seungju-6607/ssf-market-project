spring.application.name=ssf-market-project

# ===============================
# DB Info (Render / Aiven)
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# (선택) Hikari 기본 안정화
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# JPA / Hibernate (🔥중요🔥)
# ===============================
# ✅ MySQL 8이면 보통 이게 더 안전함 (MySQLDialect도 되는데, 8쪽이 덜 터짐)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# ✅ Render/운영에선 none 권장 (테이블은 DB에 이미 만들어놨으니까)
spring.jpa.hibernate.ddl-auto=none

# 로그는 필요하면 true로 바꿔서 디버깅
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# ✅ 흔한 문제: "No open ports detected" / timezone / ssl 관련 에러 방지
spring.jpa.open-in-view=false

# ===============================
# Render Port & Address
# ===============================
server.port=${PORT:8080}
server.address=0.0.0.0

# ===============================
# File Upload
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
# ===============================
# Error detail (debug)
# ===============================
server.error.include-message=always
server.error.include-exception=true
server.error.include-stacktrace=on_param

