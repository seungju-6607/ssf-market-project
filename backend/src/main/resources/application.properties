spring.application.name=ssf-market-project

# ===============================
# DB Info (Render / Aiven)
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# (선택) Hikari 기본 안정화
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ===============================
# JPA / Hibernate (🔥중요🔥)
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# ===============================
# Render Port & Address
# ===============================
server.port=${PORT:8080}
server.address=0.0.0.0

# ✅ Render 같은 프록시/로드밸런서 환경에서 https/redirect/쿠키 처리 안정화
# (Spring Boot 3.x 권장)
server.forward-headers-strategy=framework

# ===============================
# Session Cookie (Vercel ↔ Render 크로스 도메인 로그인 유지 핵심)
# ===============================
# ✅ 크로스 사이트 쿠키 허용 (SameSite=None)
server.servlet.session.cookie.same-site=None
# ✅ SameSite=None 사용 시 Secure 필수 (https 환경)
server.servlet.session.cookie.secure=true
# (선택) 쿠키 기본 경로
server.servlet.session.cookie.path=/
# (선택) 세션 타임아웃 (30분)
server.servlet.session.timeout=30m

# ===============================
# File Upload
# ===============================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# ===============================
# Error detail (debug)
# ===============================
server.error.include-message=always
server.error.include-exception=true
server.error.include-stacktrace=on_param

# JWT (발표용)
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=3600000

